{% extends 'ct-search/search.html.twig' %}

{% block content %}

  <div class="row">
    <div class="col-md-8">
      <div class="panel panel-default">
        <div class="panel-heading">
          <h3 class="panel-title">{{'cloud_search.result'|trans}}</h3>
        </div>
        <div class="panel-body color-gray">
         {{errorMessage|default('')}}
        </div>
      </div>
    </div>
    <div class="col-md-4">
      {% if type %}
        {% include 'ct-search/search-hot-' ~ type|lower ~ '.html.twig' %}
      {% endif %}
    </div>
  </div>

{% endblock %}